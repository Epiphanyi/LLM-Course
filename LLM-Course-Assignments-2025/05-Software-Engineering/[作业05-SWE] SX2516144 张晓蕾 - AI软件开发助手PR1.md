![banner](public/gemini-banner.png)

# 🤖 AI 软件开发助手（Gemini Engineer）

一个交互式、可调用本地工具的 AI 编程助手。它支持通过 LLM（DeepSeek/OpenAI/Qwen/Gemini）进行需求理解、代码生成、测试执行、文件读写与编辑、命令运行等操作，并在终端中以流式形式反馈结果。

**核心目标**
- 在一个安全可控的工作目录内，借助 AI 自动化完成编码任务
- 通过“函数调用”桥接 AI 与本地文件系统/命令行
- 支持多种主流模型与 OpenAI 兼容接口

## ✨ 功能特性
- 模型接入：DeepSeek/OpenAI/Qwen（通过兼容接口），Google Gemini
- 文件操作：读取、创建、批量创建、编辑、目录列出
- 命令执行：运行脚本、测试、构建等命令
- 流式输出：实时展示 LLM 的响应与工具调用
- 上下文管理：将文件/目录加入对话上下文以便更精准的代码分析
- 安全限制：路径校验、最大文件大小限制、二进制文件自动跳过
- 交互指令：支持常用终端指令（`/add`、`/help`、`/clear`、`/exit`）

## 🧱 项目结构
- `main.py`：主程序入口与交互逻辑、模型初始化、工具调用桥接
  - 提供商选择与分流：`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:529-536`
  - OpenAI/DeepSeek 初始化与错误提示：`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:538-560`
  - Gemini 初始化：`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:562-588`
  - OpenAI 兼容流式响应与错误打印：`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:930-963`
  - 交互主循环：`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:1089-1137`
- `demo.py`：无需外部 API 的核心功能演示（文件创建/读取/编辑、目录列出、安全检测）
- `requirements.txt`：依赖清单
- `setup.py`：打包安装配置（控制台入口 `gemini-engineer=main:main`）
- `run.sh` / `install.sh`：Linux/macOS 脚本（Windows 请用 PowerShell 步骤）
- `run_windows.bat`：Windows 启动脚本
- `public/`：静态资源（Banner、UI 预览）
- `.env` / `env.example`：环境变量配置与示例
- `test_example.py`：示例代码，便于 AI 进行分析与重构练习

## 🚀 快速开始（Windows）
- 安装 Python 3.11+
- 进入项目目录：`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent`
- 创建并激活虚拟环境：
  - `python -m venv venv`
  - `.\venv\Scripts\Activate.ps1`
- 安装依赖：`pip install -r requirements.txt`
- 填写 `.env`（见下文“配置说明”）
- 启动：`python main.py`

## ⚙️ 配置说明（.env）
以下为不同提供商的典型配置方式：

**DeepSeek（推荐）**
- `LLM_PROVIDER=openai`
- `OPENAI_BASE_URL=https://api.deepseek.com/v1`
- `OPENAI_MODEL=deepseek-coder`
- `OPENAI_API_KEY=你的_DeepSeek_API_Key`

**OpenAI**
- `LLM_PROVIDER=openai`
- `OPENAI_BASE_URL=https://api.openai.com/v1`
- `OPENAI_MODEL=gpt-4o`（或你账户可用的模型）
- `OPENAI_API_KEY=你的_OpenAI_API_Key`

**Qwen（通义千问）**
- `LLM_PROVIDER=openai`
- `OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1`
- `OPENAI_MODEL=qwen-coder-plus`（或其他可用模型）
- `OPENAI_API_KEY=你的_Qwen_API_Key`

**Gemini**
- `LLM_PROVIDER=gemini`
- `GEMINI_API_KEY=你的_Gemini_API_Key`
- `GEMINI_MODEL=gemini-2.0-flash`（或其他你可用的模型）

注意：真实密钥仅保存在 `.env`，不要提交到版本库。

## 🖥️ 交互用法
- 启动后，命令行提示符为：`🤖 gemini-engineer>`
- 可用指令：
  - `/add <file_path>`：将指定文件加入上下文
  - `/add <folder_path>`：将目录下的文本文件批量加入上下文
  - `/help`：显示帮助说明
  - `/clear`：清空会话历史并重新载入系统提示
  - `/exit` 或 `/quit`：退出应用
- 示例：
  - “创建一个 Flask 项目并提供 Hello World 接口”
  - “分析以下代码的安全隐患并修复”
  - “为此模块生成单元测试并运行”

## 🔧 工具能力（AI 可调用）
- `run_command(command)`：运行命令（安装依赖、执行脚本、跑测试等）
- `read_file(file_path)`：读取文件内容
- `read_multiple_files(file_paths)`：批量读取多个文件
- `create_file(file_path, content)`：创建/覆盖文件
- `create_multiple_files(files)`：批量创建文件
- `edit_file(file_path, original_snippet, new_snippet)`：按片段替换文件内容
- `list_directory(dir_path)`：列出目录内容

## 🏗️ 体系架构
- 主类 `PUding-Agent` 管理终端交互、会话历史与模型调用（`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:514-588`）
- 工具函数通过统一的执行入口触发，并在 UI 中展示结果（`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:1003-1035`）
- 采用流式输出，边生成边显示，并收集函数调用参数后执行本地操作（`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:971-1019`）

## 🔒 安全机制
- 路径规范化与校验，阻止越权访问（例如 `../../../etc/passwd`）
- 文件大小限制（默认 1MB）
- 自动跳过二进制/媒体文件
- 仅允许在当前工作目录及其子目录内进行操作

## 🧪 示例与演示
- 无需外部 API 的演示：`python demo.py`
  - 创建/读取/编辑文件
  - 目录列出与安全检查
  - 演示结束会清理临时文件
- 示例代码：`test_example.py`
  - 包含计算器类与常见算法（斐波那契、阶乘、素数检测），适合进行代码优化与测试生成练习（`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\test_example.py:13-66`、`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\test_example.py:67-133`）

## 🐛 故障排除
- 401 鉴权失败（DeepSeek/OpenAI/Qwen）：
  - 检查 `OPENAI_API_KEY` 是否为对应平台的真实密钥
  - 检查 `OPENAI_BASE_URL` 与密钥所属平台是否匹配
  - 错误输出位置：`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:954-963`
- 缺少 Gemini 密钥：
  - 确认 `.env` 中已设置 `GEMINI_API_KEY`
  - 初始化位置：`c:\Users\Matebook\Desktop\Master's degree\ZXLSoftware-Engineer-Agent\main.py:562-588`
- 依赖错误：执行 `pip install -r requirements.txt` 后重试
- Python 版本不兼容：请使用 3.11+
- Windows 脚本限制：PowerShell 若限制执行脚本，可运行 `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`

## 📜 许可证
- 本项目开源，遵循 MIT License（如需修改可在仓库中更新）。

## 🙌 致谢
- DeepSeek、OpenAI、Qwen、Google Gemini 模型
- `rich`、`prompt_toolkit`、`python-dotenv` 等社区库
